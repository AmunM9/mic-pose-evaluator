# OpenAI — requerido
OPENAI_API_KEY=sk-...

# Base de datos (Docker Compose la configura automáticamente)
DATABASE_URL=postgresql+asyncpg://micpose:micpose_dev@localhost:5432/micpose

# App
ENVIRONMENT=development
MAX_AUDIO_SIZE_MB=25
